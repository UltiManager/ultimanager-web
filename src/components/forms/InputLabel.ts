import styled from "styled-components";

/** A lable for a form field. */
const InputLabel = styled.label`
  color: #333;
  display: block;
  font-family: "Open Sans", sans-serif;
`;

export default InputLabel;
