import styled from "styled-components";

/** A wrapper for the components that make up a single field in a form. */
const FormControl = styled.div`
  margin-bottom: 1em;
`;

export default FormControl;
